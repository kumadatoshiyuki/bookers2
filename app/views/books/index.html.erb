<div class="row">
  <div class=col-xs-12>
  <% if @book.errors.any? %>
    <div style="color: red">
      <%= @book.errors.count %>errors prohibited this obj from being saved:<br>
      <ul>
        <% @book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  </div>

  <%= render 'users/kyoutu', user: @user, book: @book %>
    <div class="col-xs-9">



  <h2>Books</h2>
  <table class="table table-hover">
      <thead>
        <tr>
          <th></th>
          <th>title</th>
          <th>Opinion</th>
        </tr>
      </thead>
        <% @bookall.each do |book| %>
      <tr>
        <td><%= link_to user_path(book.user_id) do %>
          <%= attachment_image_tag book.user, :profile_image, :fill, 40, 40, class: "pull-left profile-thumb", fallback: "no-image-icon.jpg"%>
          <% end %>
          </td>
        <td><%= link_to book.title, book_path(book) %></td>
        <td><%= book.body %></td>
      </tr>
    <% end %>
    </table>
  </div>
</div>